spring:
  profiles:
    include:
      - mysql

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    database: mysql
    defer-datasource-initialization: true

  sql:
    init:
      mode: always